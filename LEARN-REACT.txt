<div id="root"></div> (index.html) => content nằm trong đây hết (lấy context trong index.js nhở getElementById) ==> không cần đụng vào
index.html

index.js render app.js để hiện lên web



Setup a Django Backend - React (python environment setup for windows [Caleb curry])

Download python on their website 
Windows terminal => py --version
		cd LEARN-REACT
		mkdir backend 
		py -m vent .venv
		dir -a
		.venv\Source\activate

		python -m pip install Django
		